#include <stdio.h>

int main() 
{
    char chh [20] ;
    char vowles ;
    char count;

    for 
     (int k = 0; k < 5; k++)
     { 
        if (chh[k] == 'a' || chh[k] == 'e' || chh[k] == 'i' ||
            chh[k] == 'o' || chh[k] == 'u' ||)
        
      { 
        count++;
        }
    }

    printf("Vowel count = %d", count);
  return 0;
}